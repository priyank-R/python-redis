# python-redis

A simple implementation of redis in python, referring the implementation from Arpit Bhayani's Redis Internals series.

# Helpful Commands: 
1. Connect using redis-cli using a one-off docker instance: 

`docker run --rm -it redis:alpine redis-cli -h localhost -p 7639`
